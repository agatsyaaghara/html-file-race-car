var gameState=0;
var playerCount,database;
function setup(){
    createCanvas(500,500);
    database=firebase.database();
   game1=new game()
   game1.getState()
   game1.state()
}

function draw(){
   
}

